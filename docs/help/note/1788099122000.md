# A confidential note

This is a sample page. It carries the `CONFIDENTIAL` flag so the flag can be seen doing its work:
the red stamp across the article, and the badge beside the title in every list that shows the note.
Nothing on this page is confidential.

The flag marks; it does not hide. A published export carries a flagged note like any other — the
stamp travels with it, and so does every word underneath. Keeping something out of a publication is
a job for a vault of its own, not for a marker on one note inside a vault that gets published whole.

See [[Properties]] for setting flags, and [[A deprecated note]] for the other one.
