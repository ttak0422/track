# A config file kept beside a note and embedded as a code block.
service: notes-sync
schedule: "0 * * * *"
targets:
  - name: laptop
    path: ~/vault
  - name: server
    path: /srv/vault
retention_days: 30
notify: true
